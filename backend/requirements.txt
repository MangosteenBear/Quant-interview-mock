# 量化面试刷题题库平台 - 后端依赖
# 安装: sudo pip3 install -r requirements.txt

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ORM + 数据库驱动
# 开发环境用 SQLite (aiosqlite)，生产切 PostgreSQL (asyncpg)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0

# 数据库迁移
alembic>=1.13.0

# 数据校验 + 配置
pydantic>=2.0
pydantic-settings>=2.0

# 表单解析（文件上传等）
python-multipart>=0.0.9

# 账号体系（M2）
PyJWT>=2.8.0
